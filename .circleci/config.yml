version: 2
jobs:
  build:
      docker:
        - image: circleci/openjdk:8-jdk-browsers

      working_directory: ~/spring-fortune

      steps:
        - checkout
        - run:
          name: Run tests
          command: mvn test